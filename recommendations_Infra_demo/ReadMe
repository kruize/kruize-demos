
## Usage
#
# Runs the recommendations demo with existing data 
# Uses TFB result data
./recommendations_demo.sh -c minikube --dataDrivenRecommendations --dataDir="./recommendations_demo/tfb-results/"

# Uses crc data. 
./recommendations_demo.sh -c minikube --dataDrivenRecommendations --mode="crc" --dataDir="./recommendations_demo/crc-results/"

# Runs the demo by deploying the benchmark and monitor recommendations for the benchmark
./recommendations_demo.sh -c minikube --monitorRecommendations --clusterName="localhost" --demoBenchmark

# Runs the recommendations demo by monitoring all the applications excluding openshift specific in the cluster and generate recommendations for the same.
./recommendations_demo.sh -c openshift --monitorRecommendations --clusterName="localhost"

# Runs the demo to compare the recommendations of diff recommendation logics for a given data (TODO)
./recommendations_demo.sh -c minikube --compareRecommendations
