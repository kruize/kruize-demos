## Functionalities of scripts / file

| Script / File		| Description											|
|-----------------------|-----------------------------------------------------------------------------------------------|
| `getenvoptions.py`	| Generates the JDK_JAVA_OPTIONS using the HPO Config. It includes Java and Quarkus tunables.	|
| `getobjfuncresult.py`	| Calculates the objective function result value.						|
| `utils.py`		| Functions include to get experiment_id, total_trials and generate a csv with all trials data.	|
| `runbenchmark.sh`	| Demo script to run TechEmpower benchmark.							|
| `search_space.json`	| Searchspace json used to run TechEmpower benchmark.						|

